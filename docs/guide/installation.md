# 安装

```bash
pnpm add antdv-superform ant-design-vue
```

或使用 npm：

```bash
npm install antdv-superform ant-design-vue
```

## 样式

组件库入口会加载自身样式。应用仍需按 Ant Design Vue 的版本要求引入其基础样式。

## TypeScript

包内附带完整类型声明。推荐使用 `defineForm`、`defineTable` 和 `defineDetail` 获得更准确的配置提示。
