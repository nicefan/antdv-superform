# 开发阶段记录

2026-09-21 布局修正：修复 DataInspector 与字段联动页的多语句模板解析错误；最终按用户澄清恢复左上角 SF + SuperForm 品牌，第二行显示 UI 名称；顶部页面标题、中间动态演示区、右侧占位可收缩观察区、底部向上展开测试区。移除悬浮遮挡和全局元素/组件类名样式覆盖。此前 9 个共享 Vue 模板静态编译无语法错误；本次品牌位置微调仅格式化，未执行类型检查、单元测试、浏览器交互或 install/build。详见阶段记录末节。

2026-09-21 最新接续：按用户最新要求，两个 UI 各保留一个 example 入口，改为基础表单、字段联动、容器与列表、表格操作、上传与弹窗、详情与扩展、综合示例七个功能演示页，共用右侧可控数据观察窗。旧 AntDV #0/#1/#2 的能力已按功能迁入，旧数字导航及 upgrade-dev 独立入口撤下；旧源文件中的未提交改动保留但不再参与当前入口。顺带整理示例配置、源码联调、数据层级和观察记录的一致性。新页面只做格式化与静态差异检查，未执行类型检查、测试、浏览器交互、install/build；用户自行测试后反馈。Table 编辑状态管理尚未推进。详情见阶段记录末节。

当前阶段：[v1.0.0-beta.2 输入组件与普通容器适配重构](./v1.0.0-beta.2.md)。已完成 Adapter 渲染入口、Descriptions、按钮与列表交互收敛；2026-09-21 Core 与两套 UI 包类型检查通过。局部验证发现的两处 InputList 问题已修复，现有 InputList 12 项用例及 Core 类型检查复查通过；此前 GroupList、formModel 共 3 项通过，本轮未重复运行。构建及浏览器交互未执行。具体范围和复现方式见阶段记录。

2026-09-21 接续完成 Form 公开实例与校验协议收口，以及 Table 首轮校验接入、异步保存和渲染开销修正；三处类型检查通过。本轮未执行单元测试或浏览器回归，Table 选择、分页与编辑期间外部数据变化尚未专项改造。

两套 UI 包的 uiComponents.ts 已集中管理组件渲染、默认属性与所属 service；日期时间默认属性回归 fields，按钮预设归入 actionGroup.schemaDefaults，官方 defineUIAdapter 声明不再单列 form/modal/upload/table/defaults。Core 创建 Adapter 时归一化，支持两种 override 入口。三处类型检查通过，本轮未执行单元测试、构建及浏览器验证。

字段与固定 UI 的声明 render 已统一接收上下文对象，两条路径共用属性转换；Element Plus 由导出名称目录推导同源注册，删除独立别名表。当前三处类型检查通过，单元测试及浏览器回归未执行。

2026-09-16 开始本阶段时确认最新标签为 v1.0.0-beta.1，因此使用 v1.0.0-beta.2 记录；未创建或发布标签。此前 T001–T004 及 InputGroup 改动见[升级归档](../upgrade/status/ACCEPTED-2026-09-16.md)。

每个阶段记录需求、影响范围、最终改动、可观察 API/类型/行为变化，以及必要的测试清单；被推翻的方案标为删除并保留原因。除非明确要求，不主动编写或执行测试，不将未执行项描述为通过。正式文档仅按用户明确要求同步，v1.0.0 标签推送前的站点文档只描述当前代码用法。
