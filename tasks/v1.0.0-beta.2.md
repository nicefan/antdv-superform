# v1.0.0-beta.2 开发阶段

日期：2026-09-16；更新：2026-09-20

状态：输入组件、固定渲染、Descriptions、按钮及列表容器协议调整已实现；类型检查、测试、构建均未执行，待验证及用户确认。

## 范围与已确认决策

- ~~本阶段仅涉及动态输入字段，Container 不在实现范围。~~ 2026-09-19 按用户确认追加 Group、Card、Tabs、Collapse 首轮改造；数组、Form、Table、Detail 业务逻辑和其它 capability 仍不在本轮范围。
- 真实输入组件由现有自动导入或初始化参数提供。纯名称目录与字段差异表分离；原始组件池与最终 field 配置缓存分离。
- Core 处理专项配置；仅在配置 labelField 且未配置顶层 options 时，读取 attrs.options/fieldNames 查找标签，不生成标准 options 或 Switch 专项状态。专项结果与 attrs、binding 分开传给 UI package。
- 删除字段独立 render 入口，统一用 h 渲染。原始组件只接收最终原生参数；独立适配组件显式接收 effectData、model、option、attrs、binding、state。
- UI package 配置默认 model，特殊字段只声明差异；通过 SDK 纯辅助函数提前固化转换实现，Core 不逐次解释 model。
- 字段提供 adaptProps、adaptSlots。Core 包装 slot 的业务上下文，Adapter 再适配，用户 slot 优先，生成节点保持延迟执行。
- 顶层 options 改为配置包，source 与 dictName 可同时配置，source 优先并输出警告，忽略 dictName；fieldNames、valueToNumber、labelAsValue 收入包内。labelField、stringifyValue 留在字段顶层。
- Select / SelectV2 不提供搜索专项处理；原生 onSearch、remoteMethod、过滤及 loading 配置直接透传，InputSearch 也不提供搜索专项处理。
- Input / InputSearch 均不提供 onSearch 专项包装或 loading。InputSearch 保留原生组件，原生事件直接透传；顶层事件沿用通用事件上下文机制。
- Switch 固定 options 第 0 条 unchecked、第 1 条 checked；删除 firstIsChecked、valueLabels 及从 attrs.defaultChecked 推导模型值的逻辑。初始值只由既有 initialValue 流程建立。
- 顶层 disabledDate 保留，调用签名为 disabledDate(effectData, ...原生参数)，包含 UI 提供的临时选择参数；attrs.disabledDate 原样透传，顶层配置存在时覆盖。

## 当前阶段被替换的设计

- ~~AntDV picker/range 使用 Core 生成提示。~~ 已删除；AntDV 日期、时间组件不再配置 picker/range 处理器，defaults.placeholder=undefined 清除通用兜底，交给组件库默认提示；用户显式 attrs.placeholder 仍优先。disabledDate/endField 独立处理不受影响。
- ~~范围别名同时配置 picker 和 range。~~ 已删除；Element Plus 范围别名仅配置 range，避免先生成单值提示再覆盖。

- ~~固定别名在各自 adaptProps 中覆盖模式，并再次通过 type/isRange 判断范围。~~ 已删除；新增通用 fixedProps，由 getAttrs 在 defaults、用户 attrs 和专项 props 之后统一覆盖。范围别名声明 range 处理器并直接选择范围适配，不再推断其模式。
- ~~picker 同时生成 single/range 提示，createFieldPropsAdapter 再补单值提示。~~ 已删除；picker 输出单个 placeholder，新增 range 处理器输出两端数组，getAttrs 统一合成默认提示，createFieldPropsAdapter 不再处理提示兜底。

- ~~范围别名通过 defaults 设置模式，允许 attrs 覆盖为普通模式。~~ 已删除；DateRangePicker 固定 type=daterange，TimeRangePicker 固定 isRange=true，由 Core 的 fixedProps 统一覆盖合成后的原生属性，再直接转换范围提示。普通 DatePicker/TimePicker 仍按用户配置的模式工作。

- ~~普通 picker 与范围 picker 都由 UI package 的 adaptProps 填充 placeholder。~~ 已删除；普通提示由 Core 共用属性处理补齐。AntDV 范围组件直接使用 range 输出数组，Element Plus 范围别名直接转换；只有原始组件按用户 type/isRange 判断是否转换。

- ~~component 只能配置独立适配组件，字段 key 必须对应原始组件名。~~ 增加字符串别名目标；字符串走原始组件协议，不增加 Vue 渲染边界。Element Plus 新增 DateRangePicker → DatePicker、TimeRangePicker → TimePicker。
- ~~picker 输出 modelBehavior.splitRange，由 useVModel 第三个参数接收。~~ 已删除；useVModel 直接按 endField 拆分存储，picker/range 分别提供单值/范围提示，UI 范围模式与存储拆分独立。
- ~~保留输入字段 keepField、valueToString 兼容别名。~~ 按最新范围确认，仅删除这两个输入 API 的类型和运行时兼容，使用 endField、stringifyValue。其它模块废弃 API 不动，诊断器仍保留旧写法迁移提示。

- ~~options.source 与 options.dictName 类型互斥，同时配置时抛错。~~ 已删除；类型允许同时配置，运行时警告并优先 source，不调用字典接口。空数组、Ref 暂为空或 source 函数返回空值也不回退到字典。

- ~~独立适配组件分别重复声明上下文 props。~~ 提取 SDK 公共 fieldComponentProps，Element Plus TreeSelectField 直接复用；入参和运行行为保持不变。

- ~~Element Plus TreeSelect 在 change 时递归遍历 data 查找标签。~~ 已删除；改用独立适配组件监听公开的 selectRef.selectedLabel，复用组件库选中标签及懒加载缓存，不自行扫描整树。

- ~~缺省顶层 options 时将 attrs.options/fieldNames 归一化为标准选项，并扩展到详情展示和 TagSelect。~~ 已删除；兼容仅限 labelField 查找，原生 props 保持原样，不提升为专项配置。

- ~~选项解析完全不读取 attrs；processor 自行写入 labelField；TreeSelect 保留顶层 data 别名。~~ 按 2026-09-18 用户要求，增加仅用于 labelField 查找的原生选项兼容，标签统一走 useVModel，删除 TreeFieldOption.data 及其运行时读取。Element Plus 原生 attrs.data 不受影响。

- ~~两套 Adapter 分别通过 adaptOptions 合并专项选项，Element Plus Select 使用 ElOption slot 兜底。~~ 按 2026-09-18 用户要求，标准 options 由 Core 统一合成，Select 直接使用原生 options prop；删除两套适配表的重复合并配置。~~AntDV RadioGroup 保留 label 转节点。~~ 已按后续要求删除其 adaptProps，选项直接使用 Core 标准输出。

- ~~TextArea / InputNumber 通过 adaptProps 补充默认宽度。~~ 按 2026-09-18 用户要求移到 defaults.style；原生默认属性统一由 defaults 声明，adaptProps 只保留必要协议转换。

- ~~InputSearch 保留 search processor、专项 onSearch 与 loading。~~ 按 2026-09-18 用户要求删除，同时移除 InputSearchFieldOption 和事件过滤特例。
- ~~placeholder 在 UI package 中生成。~~ 按用户要求移到 Core：processors 包含 options 或 picker 时用“请选择”，其它输入用“请输入”；picker/range 由 Core 分别输出单值/范围提示，UI package 仅在范围模式映射原生属性，普通 picker 使用 Core 共用提示，defaults / attrs（含空字符串）优先。

- ~~通过 options.search 启用 Select 远程搜索、节流和 loading。~~ 按 2026-09-18 用户要求删除，同步移除 optionsSearch 状态、source 的 keyword 参数及无消费者的选项 loading。

- ~~最终 field 缓存独立 render 函数，Core 调用 field.render。~~ 已删除，改为缓存组件、原始/适配标记及 props/slots 处理方法，Core 统一 h。
- ~~字段不再声明 model 差异，每个 UI 渲染函数自行写转换。~~ 已删除，UI package 通过默认 model 与个别字段配置提前生成转换方法。
- ~~processors 接收 attrs 并返回 transformProps。~~ 已删除，仅返回业务状态及模型行为。
- ~~维持 Input 搜索增强、Switch valueLabels 和 attrs.defaultChecked 的旧行为。~~ 按 2026-09-17 用户确认的减法方案删除，不再作为兼容要求。
- ~~本阶段所有公开输入配置保持不变。~~ 已删除；本轮包含用户确认的配置 API 变化，见下节。

## 实现与可观察 API 变化

1. src/adapter/runtime.ts 保留初始化锁定；fieldRegistry.ts 保留手动/自动注册优先级和原始池。SDK 新增 useUIComponent，只读已加载原始缓存，不触发最终 field 解析。
2. resolveUIField 缓存 type、component、adapted、defaults、processors、adaptProps、adaptSlots。移除此前新增的 FieldRender / renderField；公开 FieldState、FieldPropsAdapter、ResolvedField。
3. UIAdapter 使用 supportedFields、adaptFieldProps、fields；FieldAdapter.component 为可选独立适配组件。输入字段 defaults 与应用 configure({ defaultProps }) 分开，后者未更名。Core 使用 Vue mergeProps 合成默认属性、attrs 与标准专项 props（当前为 options）：class/style 按原生规则合并，用户同名样式优先，保留未覆盖的默认样式。
4. SDK 新增 fieldComponentProps，统一声明 type、option、model、effectData、binding、state、attrs，并保留必填类型推导；TreeSelectField 已复用。SDK 提供 createFieldPropsAdapter、defineFieldAdapters、combineFieldHandlers，供 UI package 在定义阶段固定 model 协议并组合原生监听器。定义辅助方法接受局部 model 配置，输出运行时字段不再携带 model 描述。
5. 模型绑定存在时覆盖原生值，包括 undefined；原生更新监听器仍执行原参数，不能覆盖 Core 写回。disabled 有专项结果时才覆盖，继承 true 优先于子字段 false；未配置时保留原生值。placeholder 在 Core 生成，仅用于兜底。
6. 共用 options processor 负责选项解析及递归 children 查找；通过 useVModel 的 onUpdate:labelValue 同步标签，标签序列化与路径写回集中在 useVModel。picker 提供单个 placeholder，range 提供两端 placeholder 数组；switch 只输出两种状态并复用标签绑定；tree 的原生标签参数和字段映射由 UI package 处理，再调用同一标签更新入口。
7. 公开 OptionsSource、OptionsConfig；InputSearchFieldOption 已删除。删除 InputFieldAttrs、InputFieldOption、SwitchFieldAttrs；SwitchFieldOption 不再包含 valueLabels。options 内不保留 valueToLabel 别名，顶层 dictName/valueToNumber/labelAsValue 不再作为输入选项配置读取。
8. Collections、FieldProcessorRenderer、TagInput 统一 h；表格三个内联编辑入口同步传递原生 attrs 与专项状态。
9. useOptions、详情展示、TagSelect、表格 TabsFilter 改用 options 配置包，编辑与展示共用归一化；表格展示仍在首次取得行上下文时加载选项。按钮 dropdown 的原有数据源类型保持不变。
10. OptionsConfig.fieldNames 新增 children 映射，选项递归归一化与查找覆盖子节点；原生选项兼容仅用于配置 labelField 时查找标签，详情展示和 TagSelect 不新增该兼容路径。RadioGroup 删除专用 adaptProps，不再转换 label 节点。删除 FieldState.onValueChange，树选择适配直接调用 binding['onUpdate:labelValue']。Element Plus TreeSelect 通过独立适配组件监听 selectRef.selectedLabel（仅配置 labelField 时），覆盖初始值、外部赋值及异步标签更新；单选清空标签为组件库的空字符串，多选为空数组。真实组件仍从 useUIComponent 缓存取得，element-plus 仅导入实例类型。
11. 两个产品的字段名称目录、初始化校验、名称保留及 resolver 共用纯名称目录。缺失真实组件仍明确报错。schema 自定义 render 与其它 capability 的 render 不属于被移除的字段 render 协议。

12. FieldAdapter.component 支持字符串原始组件目标；原始池解析别名，最终 field 仍按 Schema type 缓存。Element Plus 自动导入将范围别名注册到原始名称并复用导入，名称目录、全量组件表、Schema 属性和扩展类型同步加入别名；别名模式为固定配置：DateRangePicker 的 type、TimeRangePicker 的 isRange 通过 fixedProps 声明，不允许被原生 attrs 覆盖；fixedProps 是通用字段协议，不限于日期别名。
13. picker 生成“请选择{label}”，range 生成“请选择开始/结束{label}”数组，getAttrs 将其作为默认值合成。AntDV 日期/时间组件使用组件库默认提示；Element Plus 范围别名直接将 placeholder 数组映射为 startPlaceholder/endPlaceholder，分别保留显式原生提示。原始 DatePicker/TimePicker 仍支持通过原生 type/isRange 进入范围模式，此时将单个默认提示用于两端。原生显式提示和空字符串优先；不按 endField 判断 UI 范围模式。

## 配置示例（当前阶段记录）

~~~ts
{
  type: 'Switch',
  field: 'enabled',
  initialValue: 0,
  options: {
    source: [
      { id: 0, name: '停用' },
      { id: 1, name: '启用' },
    ],
    fieldNames: { value: 'id', label: 'name' },
  },
}

{
  type: 'Select',
  field: 'status',
  options: { dictName: 'status' },
}

{
  type: 'Select',
  field: 'user',
  options: {
    source: (effectData) => loadUsers(effectData),
  },
}
~~~

## 本轮示例迁移

- 按用户要求仅更新 example-antdv/src/useExForm.ts：顶层 options 使用配置包，valueToNumber/labelAsValue 移入包内；Switch 使用 false/true 顺序选项替代 valueLabels；两处 TreeSelect.data 改为 treeData。
- 姓名搜索改为 InputSearch，使用原生 enterButton/searchIcon，业务自行控制 loading；省份搜索通过 attrs.onSearch 维护响应式关键词，options.source 只接收上下文，保留动态增加候选项的功能。
- RadioGroup 的原生 attrs.options 保留；TagInput 的原生 stringifyValue 配置及已有 endField 不改动。
- 建议验证姓名搜索/加载状态、省份输入新增选项、数字选项与 labelField、Switch 显示与取值、两处树选择及标签同步。未运行测试、类型检查、安装或构建；可执行 pnpm exec vue-tsc --noEmit -p example-antdv/tsconfig.json，浏览器手动验证示例交互。

## 其它依赖及迁移边界

- Input 搜索删除后，输入字段模块不再导入 Button / ElButton。Element Plus Select 与 SelectV2 都直接使用原生 `options` prop，不再静态导入 ElOption 或生成 default slot。
- 除本轮指定的 example-antdv/src/useExForm.ts 外，正式文档、其它示例业务 Schema 和测试暂未迁移，不能将本轮代码视为它们已经验证可用。示例/调用方的旧 options 数组、顶层 dictName、valueLabels、Input 搜索增强需要按上述 API 迁移；正式文档按用户明确要求再同步。
- 当前仅删除输入相关 keepField/valueToString 及 splitRange/modelBehavior；其它业务废弃 API 不清理。测试、示例中的旧用法需迁移；正式文档本轮未修改。

## 建议调整和执行的验证（全部未执行）

遵照协作约束，未新增/修改测试，未安装依赖，未运行类型检查、测试或构建。旧测试引用的 mapUIFieldProps、renderUIField、getUIFieldAdapter、FieldRender 等接口需迁移；processors 测试应改断言 state，而不是 transformProps。

- 缓存、原始组件读取、独立适配组件上下文、零差异字段和手动注册优先。公共 fieldComponentProps 需确认 SDK 导出、必填 props 推导及 TreeSelectField 接入无变化；建议 pnpm --filter superform-element-plus typecheck，未执行。
- 仅配置 labelField 且顶层 options 缺省时读取 attrs.options/fieldNames 查标签，验证原生 options/fieldNames 引用与内容不变、无标准 options/Switch 专项状态；显式 options（包括空配置、空数组源）整体优先，不混用 attrs.fieldNames。原生 onSearch/disabledDate 无专项配置时保留；专项 false 有效覆盖。
- Core model 清空、原生更新监听器保留且只调用一次；Radio/Checkbox/Switch/Transfer 和 Element Plus 默认 model。
- TextArea / InputNumber 默认宽度、用户覆盖宽度及其它样式的保留，defaults.class 与 attrs.class 的合并。
- 本轮验证建议（未执行）：AntDV 所有日期/时间类型无生成提示，显式字符串/数组/空提示保留，disabledDate/endField 行为不变；Element Plus 范围别名仅 range 仍生成两端提示。建议 pnpm exec vitest run tests/adapter.test.ts tests/elementPlusAdapter.test.ts tests/fieldProcessors.test.ts。
- 本轮新增验证建议（未执行）：直接调用 range processor 验证两端提示；用非日期字段的 fixedProps 验证通用优先级；别名使用冲突 type/isRange 仍保持固定模式；范围提示只在 getAttrs 合成，Element Plus 别名转换不读取模式配置。建议 pnpm exec vitest run tests/fieldProcessors.test.ts tests/adapter.test.ts tests/elementPlusAdapter.test.ts。
- 本轮建议调整 picker 提示用例：普通 AntDV picker 无专用 adaptProps；Element Plus 普通模式原样返回 attrs，范围模式转换两端提示；共用默认提示及空字符串覆盖保持有效。建议 pnpm exec vitest run tests/adapter.test.ts tests/elementPlusAdapter.test.ts，未执行。
- disabled 的未配置/false/继承 true；placeholder 空字符串、options processor 使用“请选择”、同类型不同 label 的提示隔离；模型 props 不向原始组件泄漏内部上下文。
- Input / InputSearch 无搜索增强或自动 loading，检查原生 onSearch 和通用顶层事件只调用一次；Select 原生 onSearch / remoteMethod 原样透传，不自动设置过滤、远程模式或 loading；options 异步加载保持可用。
- options.source/dictName 同时配置的警告及优先级：数组（含空数组）、Ref、同步/异步函数均使用 source，字典接口不调用；仅 dictName 时正常加载。建议调整 tests/fieldProcessors.test.ts 中相关用例并执行 pnpm exec vitest run tests/fieldProcessors.test.ts，未执行。
- options 数据源、字典、字段映射、值转换、标签同步、序列化、展示一致性；补充 children 多层递归、自定义 children 字段、单选/多选、初始值/外部赋值/异步选项、仅用于标签查找的 attrs 兼容与顶层配置优先；未配置 labelField 时不读取原生选项，修改原生 fieldNames 后重算标签。
- Element Plus TreeSelect：建议补充并执行初始值、外部赋值、单选/多选/清空、嵌套节点、自定义 props.label、lazy/cacheData、异步标签更新、原生 change/ref/slots 保留以及无 labelField 时不监听标签的用例；不执行额外整树查找。全部未执行。
- RadioGroup 原生 label 直传；TreeSelect 顶层 data 不再生效、原生 attrs.data 保留；两套树选择适配通过 onUpdate:labelValue 写回且原生 change 仅执行一次。
- Switch 固定顺序、自定义值和初始值；Picker 范围拆分、disabledDate 上下文与临时选择参数。
- 本轮建议补充：范围别名只导入一次原始组件、手动原始组件注册、原始/最终缓存区分、别名面对 attrs.type=date / attrs.isRange=false 仍保持固定范围模式，普通组件仍可切换模式；AntDV 范围数组提示、Element Plus 日期各 range 模式及时间 isRange、单端提示覆盖和空字符串；endField 独立拆分，keepField/valueToString 不再生效。未新增/修改测试，未执行测试、类型检查或构建。建议迁移后运行 pnpm exec vitest run tests/fieldProcessors.test.ts tests/adapter.test.ts tests/elementPlusAdapter.test.ts tests/superFormUnplugin.test.ts，以及 pnpm run type-check。
- slots 用户配置优先及延迟执行；标准 options 在 Core 合成，专项空数组覆盖 attrs.options、无专项配置保持原生 options，Element Plus Select 使用 options prop；树选择标签与原生事件。
- TagInput 聚焦及输入；TagSelect、TabsFilter、详情与三种表格内联编辑入口。
- 消费端按需产物检查：名称目录不引入全量真实字段组件，根入口不引用 fieldComponents。

建议命令（先迁移旧测试/示例配置，以下全部未执行）：

~~~powershell
pnpm run type-check
pnpm exec vitest run tests/adapter.test.ts tests/elementPlusAdapter.test.ts tests/officialProduct.test.ts tests/formComponents.test.ts tests/field-compat.test.ts tests/fieldProcessors.test.ts tests/Switch.test.ts tests/fields.test.ts tests/superFormUnplugin.test.ts tests/uiDependencyArchitecture.test.ts
pnpm --filter superform-antdv build
pnpm --filter superform-element-plus build
~~~

## 后续独立阶段

Form 的 render/renderItem 与实例归一化、Layout、数组与详情容器协议，以及其它 capability 的薄转发清理，尚未实施。


## 2026-09-19 普通容器首轮改造

### 确认范围与职责

- 仅改造 Group、Card、Tabs、Collapse，分别输出 UIGroupState、UICardState、UITabsState、UICollapseState。
- 内容统一命名为 title、extra、content，均使用延迟函数；面板标题的图标合成、显隐过滤、disabled、上下文、编辑/详情切换由 Core 处理。Group 的按钮位置与默认对齐也由 Core 计算。
- Adapter 一次接收完整状态，自由决定 DOM、原生组件、子组件及 slot 结构；Core 不解析这四类真实组件，也不创建 TabPane、CollapsePanel。Schema 子项直接用于生成内容，不再强制改写成 UI 子组件名称。
- Group 保留自定义 component、contentAttrs、innerContent 和详情/数组复用入口；自定义 component 的 title/actions/default 协议~~由 Adapter 连接~~由 Core 默认 Group 连接。
- 保留现有 Tabs 激活项初始化与失效回退、Collapse 初始展开策略，以及原生面板的挂载行为。不调整数组补行、校验与查询逻辑。

### 当前方案与可观察变化

- ~~四类容器沿用 component/model/transformProps，tab、collapsePanel 独立适配。~~ ~~改为 containers.group/card/tabs/collapse；list、listItem、descriptions 继续使用旧 ContainerAdapter。~~ 2026-09-20 收敛为统一 render 表，tab、collapsePanel、list、listItem 协议删除。
- ~~Group 输出 actions/actionPlacement/actionAlign，面板分别输出 label/icon。~~ 统一为 extra/extraPlacement/extraAlign 与 title；图标和文字在 Core 合成，避免 Adapter 重复解释业务内容。
- ~~新增 renderUIContainerState，保留 renderUIContainer/mapUIContainerProps/getUIContainerAdapter。~~ 已统一为 getUIRender，见 2026-09-20 记录。
- 新增并导出四类 State、UIContainerItem；~~UIContainerStates、ContainerAdapters~~ 已由 UIRenderers 替代。Tabs 激活值为 string/number，Collapse 允许其数组；item key 为 string/number，不承诺原生 UI 不支持的 symbol。
- 用户通用 title/extra/default 插槽优先于生成内容；Group 同时保留 actions 别名，extra 优先，innerContent 优先于 default。原生扩展 slots 继续交由 Adapter 处理；原生同名目标 slot 与标准内容冲突时标准内容优先。
- Tabs/Collapse 的 attrs 原生受控值由 Core 激活状态覆盖；通过 Vue mergeProps 保留原生更新监听器。未使用这些受控属性的其它 attrs 继续透传。
- Group 的默认包装仅消费 class/style，剩余 attrs 继续作为 innerContent/详情配置；维持原有属性归属。自定义 component 仍使用原有 slot 协议。
- ~~AntDV 与 Element Plus 各新增 containers.ts 管理完整渲染。~~ 现统一登记到 adapter.ts，复杂实现在 components/。四类真实组件移出固定原语表；表格等 Adapter 内部使用仍保留。Element Plus 补齐 Collapse，并实现 Card 标题/操作区、Tabs extra 与 Group 布局。
- Tabs 按钮补传当前 effectData；Collapse 标题函数使用当前面板 effectData。正式文档暂不修改。

### 待验证清单（全部未执行）

- 未新增/修改单元测试，未执行测试、类型检查、install 或 build；仅检查代码差异及空白格式，不视为运行验证。
- 建议调整 tests/adapter.test.ts 的旧 tabs/collapsePanel 属性映射和固定组件池断言，改为检查四类渲染函数及完整状态；保留 list/descriptions 原协议用例。
- 建议覆盖两套 Adapter 的标题、图标、extra、content、原生 attrs/slots、原生监听器保留、Core 受控值优先；Tabs 禁用/隐藏后的激活回退及 Collapse 单/多展开。
- 建议覆盖 Group 自定义组件、顶部/底部按钮、contentAttrs、innerContent；Descriptions、Fragment、ListGroup 和详情根布局复用 Group 的行为。
- 建议覆盖面板内字段校验、延迟挂载/卸载、动态标题与 disabled 更新、编辑/详情切换；检查 Element Plus 新增 Collapse 及 extra 布局。
- 建议命令：先调整旧协议用例，再运行 pnpm exec vitest run tests/adapter.test.ts tests/elementPlusAdapter.test.ts tests/formComponents.test.ts tests/ListGroup.test.ts tests/InputList.test.ts；类型检查使用 pnpm run type-check。均未执行。


## 2026-09-19 Collections 节点与布局拆分

- 新增 useCollectionNodes.ts：在所属组件 setup 中处理 Hidden/Fragment、字段绑定、联动、编辑状态、FormItem 与继承上下文，输出带 key/hidden/content/layout 的节点描述。不创建 Row/Col。
- 新增 CollectionLayout.ts：组织标准栅格、块级分区、换行、紧凑宽度和 compactSpace/space；不建立字段绑定。使用渲染函数组织布局，避免额外组件边界影响紧凑容器的字段首尾上下文。
- Collections 保留默认组合入口与 buildInnerNode 导出，表格三个消费者无需变更；通过函数参数交付 buildInnerNode，节点模块仅类型引用，避免新增运行时双向依赖。
- 内部参数改为 layout: grid/compact/space、fieldWrapper: formItem/none、layoutAttrs，删除 Collections.compact 及对父 option.type 的 InputGroup 特判。默认使用 grid/formItem。原 nodes 插槽继续返回渲染后的 VNode 数组。
- InputGroup 显式选择 compact/none 或 space/formItem；space 模式保留原有标准 Row/Col 后再放入 Space 的行为。组级校验、规则、局部校验触发保持原实现。
- 紧凑模式保留原有不额外包装 FormItem/DataProvider 的行为。非紧凑模式继续按字段与容器语义选择包装。
- 默认 grid 的 hasWrap 从节点分组直接得出，首次渲染即可使用 Group；不再依赖此前 content 执行时才改变的状态。compact/space 模式继续直接交给对应布局，不额外自动套 Group。
- InputList 检查结论：行级 Row 仍排列内容和增删按钮，外层 Row/Col 承担 Schema 布局；本轮无确认冗余的包装，不修改 InputList。List、数组增删、模型复用和路径更新均未改动。

### 建议验证（未执行）

- 未新增/修改测试，未执行类型检查、测试、install 或 build。
- 建议补充节点层 Hidden 绑定、Fragment 继承、editable 切换、禁用规则、字段路径随数组索引变化；检查 none 模式不重复注册字段校验。
- 建议补充默认 grid 的 span/flex/gutter/block/breakAfter、InputList 默认列宽、动态 hidden，以及 isContainer 首次渲染 Group；校验 nodes 插槽仍为 VNode 数组。
- 建议覆盖 InputGroup 紧凑/非紧凑布局、attrs 传递、直接子节点首尾上下文、组级校验，以及 InputList 的对象行、普通值行、增删和重排。
- 建议命令：pnpm run type-check；pnpm exec vitest run tests/formComponents.test.ts tests/InputList.test.ts tests/ListGroup.test.ts tests/formModel.test.ts。以上均未执行。


## 2026-09-19 列表容器与编辑模式

### 最终范围

- ~~List 继续作为 Schema 类型，ListGroup 与新列表共用全部编辑模式。~~ 按最终确认，List 仅为内部公共实现，新增 CardList/TabList/CollapseList 三个 Schema 入口；ListGroup 更名为 GroupList，保留原实现，不加 titleField 或 editModal。
- GroupList 保持默认补一行、原有 labelIndex、行内新增/删除及 Group 自定义 component 包装；查看模式也沿用原行为。本轮不增加旧 List/ListGroup 名称兼容。
- 新三个列表在表单中默认直接编辑；配置 editModal 后容器内展示详情，在弹窗编辑。查看模式只展示详情，不提供内置增删编辑入口。不引入 editable、rowEditor，也不改 Table/InputList。

### API 与实现

- List.vue 按 option.type 选择三种容器，共用行模型、增删、key、索引和校验路径。行标题使用 titleField（支持点分路径）；未指定时显示序号。key 优先读取 attrs.rowKey（默认 id），缺省自动生成；标题变化不改变行身份，重复对象出现位置分别建立行模型。
- 列表标题使用 title ?? label，新增操作显示在标题旁；buttons: false 可关闭列表级操作。rowButtons 支持配置或 false；默认直接编辑提供 add/delete，弹窗模式提供 add/edit/delete。Card/Collapse 放在容器右侧，~~Tabs 使用标题 slot 承载全部操作~~；现优先使用原生增删，编辑与自定义操作仍放 extra，见 2026-09-20 记录。
- CardList 的 attrs.span 控制每张卡片的 24 栅格宽度（默认 24），避免与 Schema 顶层 span 控制整个字段所占宽度冲突。option.gutter/rowProps 控制卡片网格。
- editModal: { form?: Partial<ExtFormOption>, modalProps?: ExtModalProps }。弹窗表单默认共用 columns，也支持 form.subItems；默认新增按钮可在空数组时打开编辑。
- 弹窗使用深拷贝草稿，每次打开重新挂载 Form；保存时校验后新增或合并到原行，取消不写回。编辑按行模型身份定位，删除按稳定 key 定位，避免弹窗/确认期间重排导致写错行；编辑目标已删除时阻止保存。
- 详情与表单沿用 exclude: ['description'] / ['form']；GroupList 无这套弹窗配置。
- 新列表允许空数组，~~调用 presentation.empty / PresentationRenderType~~改为 render.empty，由官方 UI 默认 Empty 文案展示。
- Element Plus 补齐本轮依赖的 descriptions capability，消费 Core 输出字段、显隐、内容及详情跨度。普通 Tabs Adapter 新增对 item.extra 的承载，不影响未提供 extra 的 Tabs。
- 更新 components/index.ts、schemaTypes.ts、公共类型及 shared/schema-diagnostics.mjs；模型构建仍按 columns 创建列表模板，无需修改 buildModel。

### 配置示例

~~~ts
{
  type: 'CardList',
  field: 'members',
  label: '成员',
  titleField: 'name',
  attrs: { span: 12 },
  editModal: { modalProps: { width: 640 } },
  columns: [
    { type: 'Input', field: 'name', label: '姓名', required: true },
    { type: 'Input', field: 'note', label: '备注', exclude: ['description'] },
  ],
}
~~~

### 待验证（全部未执行）

- 未新增/修改单元测试，未执行测试、类型检查、install 或 build；正式文档不修改。
- 现有 tests/ListGroup.test.ts 仍引用旧 ListGroup.vue 和类型名，需要按 GroupList 迁移后执行。旧 List Schema 消费方需明确选择新列表类型，不提供静默替换。
- 建议补充：三个新列表的直接编辑/弹窗/查看模式；空数组新增、取消无写回、校验失败、嵌套数据草稿隔离、编辑中重排与删除；行身份及标题更新、Tab 激活行删除后回退、Collapse 展开 key 清理。
- 建议补充：CardList 多列及整个列表 span 的区分、容器右侧操作、标题旁新增、原生 Empty、两套 UI 详情过滤、GroupList 默认补行和自定义包装保持原样；诊断与自动导入识别新类型。
- 建议命令（先迁移旧用例）：pnpm run type-check；pnpm exec vitest run tests/ListGroup.test.ts tests/InputList.test.ts tests/formComponents.test.ts tests/adapter.test.ts tests/elementPlusAdapter.test.ts tests/schemaDiagnostics.test.ts tests/superFormUnplugin.test.ts。以上均未执行。


## 2026-09-19 example 列表测试页

- 新增 example-antdv/src/ListContainersTest.vue，在 App.vue 增加“列表容器测试”页签（/#6）。
- 覆盖 CardList/TabList/CollapseList 切换、直接/弹窗编辑、SuperDetail 查看模式、空数组、新增删除、反转顺序、弹窗期间移除行、CardList 一至三列、嵌套 titleField、exclude 的 form/description 场景及实时 JSON。
- 独立 GroupList 示例检查默认补一行、labelIndex 和自定义 component 包装；不受弹窗编辑开关影响。模式切换使用 key 重建实例，数据保留；重置恢复示例数据。
- 对两个改动的 example 文件执行 ESLint 格式修复，结果 0 errors / 0 warnings。未启动 dev，未执行浏览器验证、单元测试、类型检查或 build。
- 旧 useExForm.ts 和 InputListTest.vue 仍有 List/ListGroup 写法，本轮未迁移。
- 手动验证命令（未执行）：pnpm run dev:antdv；默认端口 5173，打开 http://127.0.0.1:5173/#6。类型检查建议 pnpm run type-check，未执行。


## 2026-09-20 Adapter 入口收敛与业务定制

### 最终分层与影响范围

- Core 保留 Schema、模型、权限、显隐、增删、确认、校验、弹窗草稿和行身份；固定 UI 通过类型化的 UIRenderers 渲染，不解析 UI 真实组件结构。
- src/adapter/index.ts 只聚合导出。原 renderUIForm/renderUIContainer/renderUIPresentation 等专用转发函数删除，统一使用 getUIRender(name)；运行时首次取用校验并缓存，初始化后不支持切换 Adapter。
- UIAdapter.render 集中声明 form/formItem、row/col/space/compactSpace、group/card/tabs/collapse/descriptions、actionGroup/tooltip/tag/checkableTag/empty、modal/upload/uploadTrigger/preview、table/tableFilter。
- form 仅保留实例校验方法；services 保留命令式提示；modal 仅保留上下文恢复；icons/upload/table 仅保留图标、特殊返回值、DOM 选择器。它们是非渲染协议，不再各有组件注册和渲染入口。
- 两套 UI 包同级放置 adapter.ts、fields.ts、schemaTypes.ts；复杂 UI 实现统一进入 components/。删除 AntDV 的 antdv 中间层、两套 capabilities.ts 及独立 containers.ts，不再保留失去调用方的 List/ListItem Adapter。
- Table 与 Upload 在本轮迁移实现位置及调用入口，不重写表格选择同步、分页或上传状态机。
- 字段解析、手动注册、自动导入、原始组件缓存保持独立。固定 UI 渲染不加入字段自动导入系统。

### 默认实现与覆盖规则

- Core 提供默认 Group；UI 包可定义 render.group，业务可通过 initialize({ overrides }) 覆盖。优先级：Schema option.component > 初始化 overrides.render.group > UI 包 render.group > Core 默认 Group。
- compactSpace 缺省时使用 render.space；其它缺失渲染在实际调用时报告 Adapter 名称和缺失项，不静默丢失界面。
- 新增 UIAdapterOverrides、extendUIAdapter。render 按项浅合并，非渲染服务整项替换；不做递归深合并。createAntdvAdapter/createElementPlusAdapter 与官方产品 initialize 均接收 overrides。
- 首次 initialize 完成后，再带 overrides 或字段组件调用会报错；无参数重复调用仍幂等。覆盖仅在初始化时合并，不在每次渲染时合并。
- props/slots 保留原生扩展入口，Core 管理的受控值优先；更新监听器通过 mergeProps 组合。业务要完整替换结构时覆盖 render 项，不通过原生事件绕过 Core 的数据流程。

### 原生 Tabs 操作

- UIAction 统一输出 visible/disabled/loading/execute，不预留未使用的 edit 动作协议；UITabsState.add 与 UIContainerItem.remove 承载原生增删请求，extra 继续承载编辑、自定义按钮及菜单。
- useButtonState 复用按钮权限、状态、提示和执行入口，按钮与原生入口使用同一动作实例；computed 替换按钮分组的 watchEffect，不为缓存行增加额外侦听副作用。
- mergeActions 返回 Promise，确认取消不执行方法；执行中阻止重复触发，方法失败向调用方传播，确认框可按原生行为保留重试。既有全局/局部按钮配置仍控制动作和确认文案；保留全局 Modal 取消/关闭回调，并隔离旧确认框关闭动画对新动作 pending 的影响。
- AntDV 使用 editable-card/onEdit，Element Plus 使用 addable/closable/onEdit；Element Plus 不同时订阅 tabAdd/tabRemove，避免重复操作。Adapter 不直接修改数组或乐观删除面板。
- TabList 默认每行仅保留删除及可选弹窗编辑；新增由原生页签入口承载，空数组时回到标题旁新增。自定义渲染/菜单不被原生图标替换。
- 当前两套原生 Tabs 没有独立的新增/关闭禁用属性，disabled/loading 时对应原生入口暂时隐藏，execute 仍有状态守卫；不禁用整个页签以免影响查看内容。如业务要求保留置灰图标，可覆盖 render.tabs 实现。
- AntDV 对数字 key 与保留前缀字符串使用稳定编码，在 onActiveChange 恢复业务 key，避免数字/字符串碰撞或增删其它行引起面板重建。原生 attrs 事件接收 UI 层 key；自定义 content 使用原生 key 协议。
- Element Plus FormItem 显式把 Core name 路径映射到 prop；原 attrs 中其它字段继续透传。

### 适配步骤与业务接入

1. 新 UI 包在 fields.ts 声明字段协议，在 fieldNames.ts 声明自动导入范围；增强字段需要独立组件时放 components/。
2. adapter.ts 使用 defineUIAdapter，按 UIRenderers 中需要的项登记渲染。简单组件直接 h，复杂的事件/插槽/子组件转换放 components/Tabs.ts、Table.ts 等，不创建第二张能力表。
3. 为实际使用的场景补齐 form 校验、services 命令式提示、modal 上下文、upload.listIgnore、table.selectors；不要用空实现冒充已支持。
4. 容器接收 Core State，消费 title/extra/content 和动作对象；原生增删调用 action.execute，不解释 Schema，不重新实现权限或确认框。原生不支持的布局由适配实现组织。
5. 官方产品包装使用 createOfficialProduct；独立 Adapter 用户使用 superForm.useAdapter(adapter)。两种方式均在渲染前一次性初始化。
6. 业务只改样式/原生属性时使用 attrs/slots；替换单个 Group 包装用 option.component；统一替换整个项目的容器外观用 initialize.overrides.render。

~~~ts
import { h } from 'vue'
import superForm from 'superform-antdv'
import BusinessGroup from './BusinessGroup.vue'

superForm.initialize({
  overrides: {
    render: {
      group: state => h(BusinessGroup, state.attrs, {
        title: state.title,
        actions: state.extra,
        default: () => h('div', state.contentAttrs, state.content()),
      }),
    },
  },
})
~~~

### API 迁移与待确认边界

- ~~components/layout/containers/actions/presentation/preview 组成多组 capability~~替换为 render。删除 AdapterComponent、LayoutAdapter、ContainerAdapter、ActionAdapter、PresentationAdapter、PreviewAdapter 及旧分发表类型；四类容器 State 保留。
- UI 包旧 capabilities 工厂/常量不再导出，改用 create*Adapter({ overrides })；原生字段 components 初始化参数保留原义，不与固定渲染混用。
- 按已确认的当前阶段直接替换协议，不额外维持旧 SDK 兼容层。如存在仓库外自建 Adapter，需要按上述映射迁移；是否有此类消费方可由用户补充。
- 正式架构总览、README、AI_GUIDE 与站点文档未提前同步。本节承担当前阶段适配说明。

### 建议验证（未执行）

- 未新增或修改单元测试，未执行测试、类型检查、install、build 或浏览器交互验证。ESLint 仅按本轮要求格式化；不将格式结果视为行为验证。
- 现有 tests/adapter.test.ts、elementPlusAdapter.test.ts、officialProduct.test.ts 含旧 capability 或文件路径，需要迁移到 render 和 overrides 后再运行。此前 ListGroup 测试迁移项仍待处理。
- 建议补充：Group 四层优先级；初始化重复调用与覆盖锁定；缺失能力错误及 compactSpace 回退；固定渲染不进入字段自动导入；普通容器原生 attrs/slots/监听器。
- 建议补充：两套 Tabs 原生新增/删除、取消/失败不删除、重复点击、角色隐藏/禁用、异步 loading、弹窗保存/取消、空数组新增、查看模式、自定义按钮/菜单、数字和字符串 key、删除激活行后的回退。
- 建议回归：原按钮分组 limit、tooltip、loading、自定义 onClick 对内置方法的调用；Form/InputGroup 局部校验；Table/Upload/Modal/Descriptions 原行为。
- 可用命令（均未执行）：pnpm run type-check；pnpm --filter superform-antdv typecheck；pnpm --filter superform-element-plus typecheck。
- 用例迁移后建议：pnpm exec vitest run tests/adapter.test.ts tests/elementPlusAdapter.test.ts tests/officialProduct.test.ts tests/formComponents.test.ts tests/ListGroup.test.ts tests/InputList.test.ts。
- 手动验证可使用 pnpm run dev:antdv 的 /#6 列表页；本轮未启动服务。

- ESLint 格式化结果：62 个变更源码文件中修复 37 个；仍报告 1 个既有错误（src/exaTypes.d.ts 的 SwitchFieldOption 空接口）与 30 个警告，未清理范围外规则问题；git diff --check 无空白错误。未据此声明类型或行为已验证。
- 收尾修改的 5 个文件再次执行 ESLint：0 errors / 0 warnings；其余文件未重复扫描，前述既有规则问题仍保留。

## 2026-09-20 Descriptions、按钮与列表交互收敛

### 最终实现

- Descriptions 的字段过滤、配置继承、显隐、分组、换行和逻辑跨度统一由 Core 计算并输出明确 State；最后一项隐藏时仍提交此前积累的尾组。两套 UI Adapter 只换算原生属性和组织结构，不再各自实现一套业务分组。
- 按钮组删除 `buttonType`、`buttonShape`、`size`，统一为当前 UI `ButtonProps` 类型的 `buttonProps`。公共属性按 UI 默认、业务全局、组级、单按钮依次覆盖；单按钮 `disabled/loading` 纳入 Core 执行守卫，`onClick` 仍由 Core 统一执行。
- 内置动作的颜色与默认外观移入 UI 包 defaults；AntDV 行按钮默认 `{ type: 'link', size: 'small' }`，Element Plus 默认 `{ link: true, size: 'small' }`。GroupList、InputList、CardList/TabList/CollapseList、Table 行操作和 SuperButtons 已迁移；~~搜索展开按钮继续在 Core 写原生 attrs~~，收尾时改为 expand 动作，由 UI 包提供默认外观。
- Element Plus 补齐 Container、Action、Modal、Upload 的 UI 类型注册，`buttonProps` 和相关 Schema 属性直接获得当前 UI 类型。
- 列表标题旁新增固定头插；当前行新增固定插在当前行后。弹窗新增只在保存成功后插入，并以稳定行身份重新定位锚点；锚点已删除时报错，不静默追加。
- TabList 新增后激活新行，CollapseList 新增后展开新行；删除激活项后回退相邻项。三个列表继续共用内部 `List.vue`，CardList 默认 `gutter: [16, 16]` 保留。
- ~~Tabs 自动设置 AntDV `editable-card` 或 Element Plus `addable/closable`，并通过原生 `onEdit` 执行增删。~~ 已按最终方案删除。TabList 当前行新增、编辑、删除统一放在 tab bar 右侧并随激活行切换；标题旁新增仍可在空数组和非空数组时头插。
- 删除仅为原生增删引入的 `UITabsState.add`、`UIContainerItem.remove` 和未使用的 `UIAction` 出口。通用 `extra`、普通 Tabs 的 attrs/slots 和受控 activeKey 保留。
- AntDV Tabs/Collapse 直接接收随状态更新的 `items` prop，保留稳定业务 key；Collapse 使用原生 `change` 更新展开状态。Element Plus 用包内包装和 `ResizeObserver` 测量操作区，在 top/bottom/left/right tab bar 中动态预留实际空间。

### 可观察 API 与行为变化

- `ExtButtonGroup.buttonType/buttonShape/size` 删除，迁移为 `buttonProps.type/shape/size` 或当前 UI 对应属性。单按钮 `attrs` 仍覆盖组级 `buttonProps`。
- TabList 不再改变原生 Tabs 类型，不再暴露原生新增/关闭图标；行操作位于 tab bar 右侧。标题旁新增为头插，行内新增为后插。
- 弹窗新增期间若插入锚点被删除，保存明确失败；重排行不改变插入目标。新增及删除会同步 Tab/Collapse 的激活或展开状态。
- Descriptions Adapter 接收 Core 已归一化的 State；仓库外自建 Adapter 需要按新的 `UIDescriptionsProps` 渲染 `rows`。

### 建议验证（全部未执行）

- 未新增或修改单元测试，未执行测试、类型检查、install、build、dev 服务或浏览器交互验证。
- 建议补充 Descriptions：动态显隐后尾组不丢失、breakAfter、table/form/default、横纵布局、跨列、配置继承，以及 AntDV/Element Plus 原生属性映射。
- 建议补充 Buttons：四层外观优先级、两套 UI 默认行按钮、单按钮 disabled/loading 执行守卫、确认取消/失败及关闭动画 pending 隔离、SuperButtons 属性迁移。
- 建议补充三个列表：标题头插、行后插、弹窗期间重排/删除锚点、空数组、添加后激活/展开、删除激活项回退，以及 Tabs 普通 attrs/slots 和四个 tabPosition 下操作区尺寸变化。
- 建议命令：`pnpm run type-check`；`pnpm exec vitest run tests/adapter.test.ts tests/elementPlusAdapter.test.ts tests/formComponents.test.ts tests/ListGroup.test.ts tests/InputList.test.ts`；手动验证 `pnpm run dev:antdv` 后打开 `http://127.0.0.1:5173/#6`。以上均未执行。


## 2026-09-21 本轮接续收尾

- 延续已确认范围，未再次分派任务；保留现有已提交实现，只修正本轮收尾问题。
- 新增内部 mergeButtonConfig，GroupList、InputList、三种列表及 Table 行操作共用逐属性合并规则。局部只指定 buttonProps.size 时仍继承当前 UI 的默认行按钮风格，不再在各容器复制合并逻辑。
- 搜索展开按钮命名为 expand，默认外观登记在两套 UI 的 defaults.ButtonActions.expand；Core 不再写 type: link。AntDV 分隔线默认判断同时识别原生 variant/type，显式 divider 优先。
- 原生 buttonProps/attrs.disabled 与动作禁用共同限制 execute，避免内置 edit/delete 的默认条件覆盖显式原生禁用。删除原生 Tabs 增删移除后已无调用方的按钮 exclude 参数。
- UIDescriptionItem 增加稳定 key，来源为原始字段顺序，Element Plus 不再以行内索引或动态分组位置标识字段。标准栅格 colProps 的缺省跨度、flex/span=0 归一化移入 Core，AntDV 只消费结果；Element Plus 显式换算当前 UI 支持的 size。
- Element Plus Tabs 初次挂载立即测量操作区，后续由 ResizeObserver 更新并在卸载时释放；侧边栏同时预留操作区宽度，水平栏预留高度。未执行浏览器视觉验证，四个 tabPosition、动态按钮宽度及窄屏仍需人工检查。
- ESLint layout 格式化覆盖 23 个相关文件，修复 3 个文件格式；报告 1 个既有错误（exaTypes 的空接口）及 22 个警告，未顺手清理。未新增或修改单元测试，未执行测试、类型检查、install、build 或 dev。
- 建议补充/调整：公共 buttonProps 与单按钮 attrs 优先级、只覆盖 size、内置动作禁用、搜索展开原生样式、详情隐藏/换行后字段 key、TabList/CollapseList 连续增删及弹窗期间重排、Tabs 右侧操作区尺寸变化。先迁移旧 Adapter/ListGroup 用例，再执行以下命令，均未执行：
  - pnpm run type-check
  - pnpm --filter superform-antdv typecheck
  - pnpm --filter superform-element-plus typecheck
  - pnpm exec vitest run tests/adapter.test.ts tests/elementPlusAdapter.test.ts tests/formComponents.test.ts tests/ListGroup.test.ts tests/InputList.test.ts

## 2026-09-21 低成本验证收口

### 范围与修复

- 用户确认以代码检查、类型检查和必要的现有局部用例为主，不新增测试文件；不迁移整套旧 Adapter 测试，不 install/build。
- 开始前按要求拉取 origin/next-doc：3a42bef 快进到 e424c92，无冲突，本地修改保留。
- 修复类型检查发现的协议衔接：SDK 导出 UIDescriptionItem；原生 DOM 的 VNodeChild 用子节点数组接收；按钮内部用 Obj 读取原生 disabled/loading，公开配置仍保留各 UI 注册类型；FormItem 的规则在 Adapter 边界映射到原生类型，Element Plus 的数组路径成员转为字符串。
- Collapse 的 activeKey 保留传入 Ref 的身份，仅修正类型声明；Tabs/Collapse 按钮插槽补充已由条件判断保证的非空类型。
- 现有 tests/ListGroup.test.ts 只将导入、Schema 类型和描述迁移为 GroupList，保留原有断言及文件名。

### 实际验证结果

- 通过：pnpm run type-check；pnpm --filter superform-antdv typecheck；pnpm --filter superform-element-plus typecheck。最后调整 Collapse Ref 类型后仅复查 Core，仍通过。
- 局部用例：tests/formModel.test.ts 2/2 通过；tests/ListGroup.test.ts 1/1 通过；tests/InputList.test.ts 10/12 通过，总计 13 通过、2 失败。
- 首次使用默认 Vite 配置执行局部用例长时间停留在加载阶段，已中止，不能计为通过。实际结果来自内存配置：去掉构建专用插件、superform/sdk 指向 src/sdk.ts，避免读旧 dist；没有修改仓库 Vite 配置或新增测试文件。复现命令（PowerShell）：

```powershell
@'
import { startVitest } from 'vitest/node';
import vue from '@vitejs/plugin-vue';
import vueJsx from '@vitejs/plugin-vue-jsx';
import { resolve } from 'node:path';
const ctx = await startVitest('test', ['tests/ListGroup.test.ts', 'tests/InputList.test.ts', 'tests/formModel.test.ts'],
  { watch: false, threads: false, config: false },
  { configFile: false, plugins: [vue(), vueJsx()], resolve: { alias: [{ find: 'superform/sdk', replacement: resolve('src/sdk.ts') }] }, test: { deps: { inline: [/antdv-next/, /@v-c/] } } });
await ctx?.close();
'@ | node --input-type=module
```

- ESLint layout 格式化覆盖本次 13 个修改文件，修复 2 个文件；0 errors / 3 warnings（非空断言规则提示）。未清理其它既有 lint 问题。
- 未执行：整套测试、构建、dev 服务和浏览器交互。TabList/CollapseList 连续增删、弹窗锚点、Descriptions 动态布局和 Tabs 操作区位置仍需交互回归，不能由类型检查和本轮数组用例替代。

### 发现的问题（待确认，不扩大本轮修改）

- InputList 空数组初始化：数据已补一条，但 watcher 提前 return，首次 render 仍为空；旧测试要求初始化立即可见。建议让本轮 watcher 继续生成行模型，复查默认一行和清空数组两个场景。
- InputList 的 $index 原始值数组被外部头插：复用时读取随数组下标变化的 computed refData，而非变更前快照，导致旧行 key 匹配错误。建议保存上一轮值快照，仅在结构变化时匹配；保留普通编辑按槽位复用及内部增删语义。复查头插、删除、重复值和原地编辑。
- 上述两个分支在拉取后的 HEAD 中已存在，并非本轮按钮合并或类型修复引入。~~尚未修复，待用户确认后定向复查。~~ 用户确认后已按下节修复，未修改失败断言或新增测试文件。

## 2026-09-21 InputList 行管理定向修复

- 范围仅为 InputList 的空数组首帧与原始值数组外部插入后的身份保持；不调整对象行管理及公开配置。
- 空数组补齐默认一条后继续生成行模型，首次 render 即可显示，不再等待下一轮 watcher。
- 原始值行记录上轮值快照；监听数组各槽位以同步普通编辑后的快照。外部长度变化时按快照建立值到行的匹配表，重复值依原顺序逐个消费；保留 Object.is 对 NaN、正负零的匹配语义，避免读取已随下标偏移的 computed 值。
- 结构变化匹配由逐行 findIndex/splice 的最坏 O(n²) 改为 O(n)；匹配表仅在原始值数组长度与行缓存不一致时建立。普通编辑继续按槽位复用，内部按钮增删仍提前调整行缓存，不改变重复值行的指定删除语义。
- 边界保持：原始值没有独立业务身份，外部等长替换仍按槽位复用；重复值外部增删无法识别是哪一次出现，只保证确定的顺序匹配和独立 key。
- 已执行：现有 tests/InputList.test.ts 12/12 通过（使用上一节内存配置，仅将文件列表设为该文件）；pnpm run type-check 通过；该源码 ESLint layout 格式化检查 0 errors / 2 warnings，未清理既有警告。测试直接调用 setup 产生的两条 inject 上下文警告保留。
- 未新增或修改测试文件，未重复运行已通过的其它用例及 UI 包检查，未 install/build 或启动浏览器。若后续补充交互回归，重点检查清空后补行、编辑后再头插、重复值增删与焦点保持；这些额外交互本轮未执行。

## 2026-09-21 Form 协议与 Table 首轮收敛

### 授权与范围

- 用户确认 Form 统一公开方法、原生实例改为 getNativeInstance() 的调用边界，并要求继续下一轮 Table。
- 本轮只处理表单实例与校验服务、提交拦截生命周期，以及 Table 的服务调用、异步保存和明显渲染开销。不重写模型初始化、选择、分页或请求状态机。

### 最终改动与可观察变化

- Form 不再将原生实例属性合并到公开对象。公开 dataSource、submit、validate、validateField、clearValidate、setFieldsValue、resetFields、getNativeInstance；useForm 同步提供新增方法。原生专属方法须改为 form.getNativeInstance().原生方法；useForm 返回的 getNativeInstance 是异步方法，需要 await。
- validate/validateField 成功时返回 void；submit 保留深拷贝提交数据和事件语义。未挂载或卸载后 validate 明确拒绝；字段校验仍保留未挂载、ignoreRules 和空路径保护。
- 官方 Adapter 将字段校验失败转换为 FormValidationError，包含 fields（path/messages）、message 和 cause；异常原样保留在 cause，非校验运行异常原样抛出。类型及错误类从 Core 根入口和 SDK 导出。Element Plus 整表错误路径来自原生字段字典，路径段为字符串；指定字段校验保留传入路径。
- FormAdapter 不新增重复能力入口，继续使用 validate/validateField/clearValidate；第三方 Adapter 应遵循相同错误约定。原生校验返回值不再作为 Core validate 的返回值。
- Form 使用 shallowRef 保存原生实例，卸载和 ref 清理时释放引用；提交拦截注册返回注销函数，Upload 在作用域销毁时注销；表单卸载时清空拦截集合。
- Table 行内编辑和 SuperTable 整表校验通过 form service 执行；Core 不再读取原生 errorFields。行内字段退出或卸载时删除缓存的原生表单实例。
- 行内保存 await 持久化监听器后再退出编辑，并用 saving 阻止重复保存与保存期间取消。失败继续拒绝，不再吞掉异步错误；新增保存交付草稿快照，不在持久化前提前覆盖展示记录。取消新增按记录身份定位，避免未传 index 时误删第一行。
- 更新列配置时只创建一次 table key，避免每次渲染生成 Symbol 造成整表重建；全表编辑单元格直接传递当前 rules 值，不在 render 中新建 reactive 包装。

### 验证与后续边界

- 已执行并通过：pnpm run type-check；pnpm --filter superform-antdv typecheck；pnpm --filter superform-element-plus typecheck。ESLint layout 覆盖本轮 12 个文件、格式化 1 个文件，0 errors / 4 warnings；未清理既有警告。
- 未新增或修改单元测试，未执行本轮单元测试、build、install 或浏览器回归。上轮 InputList 通过记录不能作为本轮 Form/Table 的行为验证。
- 建议后续按需调整现有 adapter.test.ts、elementPlusAdapter.test.ts 的旧协议用例，覆盖校验成功、统一字段错误、普通异常透传；迁移后可执行 pnpm exec vitest run tests/adapter.test.ts tests/elementPlusAdapter.test.ts，当前未执行，旧用例尚未整体迁移。
- 建议现有 example 页面手动检查：公开方法与原生实例访问、Upload 动态卸载后提交、行内保存成功/失败/取消、自定义 onSave 返回 false、列更新后普通渲染不重建。可使用 pnpm run dev:antdv 和 pnpm run dev:element-plus，均未启动。
- Table 编辑期间外部数据重排/替换仍沿用旧 watcher 策略，插入仍依赖 index；选择/分页同步、弹窗 onSave/onCancel 拦截语义，以及 SuperForm 异步配置挂载时的 expose 生命周期留待后续专项分析，未宣称 Table 全面改造完成。

## 2026-09-21 内部 UI 组件声明配置

- 按用户最终确认，文件命名为 uiComponents.ts，取代讨论中的 renderers.ts 命名；两套官方 UI 包保持同层级结构。adapter.ts 组装声明配置、字段、服务和 Schema 默认配置，不再承载固定组件渲染表。
- UIComponentDefinition 按对应 UIRenderers 键推导回调参数。component 与 render 在类型上互斥。~~render 沿用原参数、不接收 defaults/adaptProps~~；最终统一接收上下文，两个分支共用 defaults/adaptProps，见本阶段后续记录。
- component 分支调用顺序：浅合并 defaults 与调用方 props（调用方优先，显式 undefined 同样覆盖），再执行 adaptProps，最后 h(component, props, slots)。不额外包 Vue 组件，不合并或改写插槽。State 的复杂转换仍由 render 显式完成，不猜测组件是否属于扩展组件。
- defineUIAdapter 接收 uiComponents 并在创建阶段归一化为现有 render 表，不保留第二份声明表；旧 render 输入仍可使用。getUIRender 的缓存和 Core Group fallback 不变。无 defaults 时直接复用传入 props，减少属性复制。
- 业务可以通过 overrides.uiComponents 按项整体替换声明，defaults 不跨两个定义深合并；同时存在 overrides.render 时，同名 render 最终优先。直接定义 Adapter 时的显式 render 也优先于 uiComponents。
- SDK 和 Core 根入口导出 UIComponentDefinition、UIComponentDefinitions、UIAdapterDefinition 类型。没有增加字段自动导入入口，也没有将固定组件混入 fields。
- ~~原有 Schema 层 defaults 保持现有作用域，未搬进组件级 defaults。~~ 用户指出未完整收口，后续按下节统一归属。AntDV FormItem 原规则类型断言随直接 component 映射移除；Element Plus FormItem/Tooltip 用 adaptProps 承接现有属性换算，其余复杂渲染迁移时保留原逻辑。
- 已执行：Core、superform-antdv、superform-element-plus 三处类型检查通过；ESLint layout 检查 9 个文件、格式化 2 个，0 errors / 1 warning；git diff --check 无空白错误。随后仅调整 defineUIAdapter 重载顺序，使同时提供 uiComponents/render 的调用优先匹配声明入口，未重复执行检查。
- 未新增或修改测试，未执行单元测试、install/build/dev 或浏览器验证。建议按需迁移现有 adapter.test.ts 后覆盖默认属性优先级、adaptProps 调用顺序、render 入参/插槽透传、两类 override 优先级及 Group fallback；命令 pnpm exec vitest run tests/adapter.test.ts，当前未执行。手动可用现有两套 example 的 Form/Tabs/Collapse/Table 页面检查迁移后的外观与交互，本轮未执行。

## 2026-09-21 UI 声明完整归属收口

- 用户指出上一轮只移动 render 表、仍在 defineUIAdapter 中重复配置 form/defaults，要求完整处理；本轮同步处理两套 UI 包。
- form 的 component 与 validate/validateField/clearValidate 放在同一声明（service）；modal 的上下文、upload 的 listIgnore、table 的 selectors 同样归入对应 service。与组件无关的 message/confirm/info 服务和图标仍由 Adapter 组装。
- FormItem 的默认属性、AntDV Table.size 放入各自 defaults；AntDV 日期/时间 valueFormat 放回对应 fields.defaults，删除 fields 末尾独立的默认表及 antdvDefaults/elementPlusDefaults 导出，避免同一字段两处维护。依赖这些旧导出的外部代码需迁移到组件或字段声明，本仓源码无调用方。
- ~~defaults 先浅合并到协议第一个参数，render 不同时使用 adaptProps。~~ 后续统一为先合成上下文 attrs，再做 adaptProps，最后分发 component/render；结构型协议明确从 state.attrs 取得原生属性，不通过函数签名猜测。
- 按钮的 rowButtons/ButtonActions 属于 Schema 预设，集中于 actionGroup.schemaDefaults，归一化时投影到运行时 defaults，不作为原生 props 下发。schemaDefaults 仅允许 actionGroup，service 按键限定对应协议类型。
- 声明类型 UIAdapterDefinition 禁止顶层 form/modal/upload/table/defaults。归一化后的 UIAdapter 内部仍保留 form/render/defaults 等访问路径，避免消费者重复解析配置；这是运行时结构，不再作为官方包的重复配置来源。
- overrides.uiComponents 中的 service 整项覆盖；仅覆盖渲染时保留已有 service。显式旧 service override 最后生效；渲染继续以 overrides.render 优先。组件默认值随该项声明替换，按钮 Schema 预设按顶层键替换，不深合并残留属性。
- 已执行通过：pnpm run type-check、pnpm --filter superform-antdv typecheck、pnpm --filter superform-element-plus typecheck。ESLint layout 格式化 9 个文件，修复 8 个文件格式，0 errors / 1 warning；git diff --check 无空白错误。
- 未新增/修改/执行单元测试，未 install/build 或运行浏览器。建议后续针对既有 Adapter 用例迁移后运行 pnpm exec vitest run tests/adapter.test.ts tests/elementPlusAdapter.test.ts，覆盖组件 defaults 覆盖顺序、service 覆盖继承、按钮预设、日期时间默认格式及显式业务配置；当前未执行，不将类型通过视为交互验证。

## 2026-09-21 字段与固定 UI 统一渲染上下文

### 最终协议

- 用户确认继续使用 render 命名，render 是函数而非 Vue 适配组件标记。字段与固定 UI 统一接收含 type/attrs/state/slots 的对象；字段额外提供真实 option/model/effectData/binding，不给固定 UI 伪造空字段上下文。
- component 与 render 互斥；component 仅表示原生组件，不再兼作字符串别名或扩展组件标记。defaults、属性/绑定转换、slots 转换均在分发前完成，字段 fixedProps 最后覆盖；适配 render 不再重复合成原生绑定和事件。
- UI 声明及 overrides.render 使用 UIComponentRender/UIComponentRenders；UIRenderers 保留为 Core 内部已归一化调用表。Group/Card/Tabs/Collapse/Descriptions 按明确协议从 attrs 获取原生属性，其它入口使用属性对象；转换发生在统一边界，不猜测参数或函数形状。
- 简单 Tag、CheckableTag、AntDV Modal 改用 component/adaptProps；Element Plus Tag 保留必要的插槽组织，属性转换独立到 adaptProps。两套包的自定义 render 均迁移为上下文对象参数；现有组件内辅助渲染函数仅由该入口调用。
- 删除 ResolvedField.adapted，Collections、FieldProcessorRenderer、TagInput 统一调用字段渲染入口。自绘 render 不强制要求原生组件，引用原生组件的扩展继续使用原始组件缓存。
- Element Plus TreeSelect 改为字段 render 调用适配组件，移除组件内重复的 createFieldPropsAdapter；树数据、标签状态监听和原始 ref 合并保持原实现。

### 组件复用目录

- elementPlusFieldImports 保留 Schema 名到真实导出名的映射：DatePicker/DateRangePicker 都指向 ElDatePicker，TimePicker/TimeRangePicker 都指向 ElTimePicker。
- 删除 elementPlusFieldAliases 及 fields 中字符串 component 配置。纯名称目录推导 elementPlusFieldSources，同一导出名第一次出现的 Schema 名为注册入口，自动导入和运行时复用同一结果。
- 同源字段共享原生组件；手动注册优先自动导入，原始注册名优先其它同源名，冲突结果不依赖首次渲染顺序。仅手动提供 DateRangePicker 时 DatePicker 也可复用；各自 defaults/processors/fixedProps 仍独立。外部业务不能再利用同源别名注册不同原生实现，差异应放各字段 component/render。
- SDK/Core 根入口导出 RenderContext、FieldRenderContext、UIRenderContext、UIComponentRender、UIComponentRenders。业务自定义旧 render(props, slots) 或 render(state) 须迁移；旧适配组件 component 配置改为 render(context) 调用组件。

### 验证

- 已执行：pnpm run type-check、pnpm --filter superform-antdv typecheck、pnpm --filter superform-element-plus typecheck，最终全部通过。初次 UI 检查发现迁移时的 tableFilter 拼接语法问题，已修复后复查通过。
- ESLint layout 覆盖 18 个文件、格式化 9 个，0 errors / 5 warnings；未清理其它既有规则提示。git diff --check 无空白错误。
- 未新增或修改单元测试，未执行单元测试、install/build 或浏览器验证。建议按需迁移现有 fields、elementPlusAdapter、superFormUnplugin 用例后运行 pnpm exec vitest run tests/fields.test.ts tests/elementPlusAdapter.test.ts tests/superFormUnplugin.test.ts；当前未执行。
- 重点回归范围：原生/自绘渲染上下文、defaults/adaptProps/fixedProps 优先级、TreeSelect 更新事件只触发一次及 labelField、Slots 延迟执行、同源字段任一手动注册及自动导入去重、两套 UI Tabs/Collapse/Table 渲染。上述交互回归未执行。

## 2026-09-21 定向人工测试页与 Collapse 渲染修复

### 最新授权与边界

- 用户起初要求优先使用现有 example 做统一渲染协议定向交互验证，之后确认允许最小调整现有 example，再明确改为“只需要生成全方位测试页面，由用户自行测试并反馈”。此后停止代做浏览器交互验证。
- 保留开始时全部未提交改动；未新增测试文件，未修改已有单元测试。本轮只扩展现有 example、维护任务记录，以及修复转向人工验证前已实际复现的 Collapse 问题。
- 不重新设计架构、不修改正式文档；Table 编辑期间外部数据变化、插入位置、选择与分页同步只提供复现入口，尚未改造业务状态管理。

### 页面与操作范围

~~AntDV 保留普通 example 与 upgrade-dev 两个入口，Element Plus 使用集中测试页。~~ 用户随后明确每个 UI 只保留一个功能演示入口；下表仅记录前一方案，已被本文末节替代，旧链接不再作为访问入口。

| 入口 | 内容 |
| --- | --- |
| http://127.0.0.1:5173/upgrade-dev/index.html#p002-real-component-names | AntDV 单选/多选 TreeSelect、外部赋值/清空、异步标签更新、原生与 Schema 事件计数、日期时间范围拆分、提示/默认宽度/插槽 |
| http://127.0.0.1:5173/upgrade-dev/index.html#p003-containers | Tabs 显隐/禁用后的激活回退、Collapse 单/多展开、默认属性与业务插槽；复用现有 ListContainersTest 的增删、重排、弹窗、查看及锚点操作 |
| http://127.0.0.1:5173/upgrade-dev/index.html#p006-upload-modal | Form 整体/局部校验、统一 fields/path/messages/cause、原生实例访问；Upload 挂载切换、提交等待、失败重试与五种上传模式 |
| http://127.0.0.1:5173/upgrade-dev/index.html#p007-table | 行内保存失败/延迟/重试/取消、onSave/onCancel 返回 false、整表校验；外部重排、同 ID 对象替换、删除目标或插入锚点、缩短/清空数据与选择分页事件 |
| http://127.0.0.1:5174/ | Element Plus 对应字段、容器、Form/Upload、Table 操作与实时数据；自动注册/手动原始名/手动范围名可切换 |

- Element Plus 的 dev alias 指向当前 Core/Adapter 源码，补充 JSX 插件；业务构建仍走包 exports，未执行构建。自动导入 resolver 使用当前源码。
- 两套验证入口通过 overrides.uiComponents 设置 Tabs 默认 left、Collapse 默认 accordion=true；页面显式 top/false 用于检查统一渲染边界的覆盖规则，不改变普通 AntDV example 的初始化。
- Element Plus 手动注册组件增加可见来源标记；只手动提供原始名或范围名时，四个日期时间字段均应显示对应标记，方便检查同源复用与手动优先。自动导入去重需在开发者工具检查虚拟注册模块，未在页面伪造检查通过。
- AntDV 旧 P001 页面保留文件，但从当前测试导航移除；当前各分区按需加载，避免未迁移的历史接口阻断其它页面。
- 保存和上传使用本地延迟/失败模拟，不调用真实业务接口。Table 新增区分模拟 API 与本地数组模式：前者模拟成功后追加数据，后者用于观察 Core 插入锚点行为，避免示例接口掩盖插入位置问题。
- 页面实时输出模型、事件计数、错误详情和最近 20 条 Table 操作日志；重置整页、切换注册/新增模式会重载，列表弹窗/查看模式切换重建表单但保留数据。上传卸载验证使用独立 hidden 开关，不重建父表单。

### 已复现问题与修复

- AntDV Collapse 的 items 接收节点，直接传递 title/extra/content 函数会显示函数字符串，导致标题、内容与行按钮失效。
- packages/superform-antdv/src/components/Collapse.ts 将三类内容包装为函数组件 VNode，保留延迟求值、响应式依赖追踪和面板懒挂载。不改变 Core 容器状态协议，也不新增公开 API。
- 转向人工测试页前，在原 /#6 页面实测：TabList 头插后激活及删除后回退；修复后的 CollapseList 标题/按钮渲染、头插自动展开、字段到标题同步及删除后相邻展开回退正常。这些局部结果不代表后来扩展的测试页面或其它边界已通过。

### 验证状态及下一步

- 已执行：指定文件格式化、静态差异检查；没有将格式化结果视为类型或行为验证。
- 两个 dev 启动尝试均因 5173/5174 已占用退出；随后使用已有服务打开过页面，未新建隐藏后台进程。不能将启动失败记作服务启动成功。
- 未执行：最终人工测试页的完整交互验证、单元测试、类型检查、install、build。用户将自行操作页面并反馈；收到反馈后只修复具体问题。
- 服务未运行时可用：`pnpm run dev:antdv`、`pnpm run dev:element-plus`。按项目约束应在可见集成终端启动，分别使用 5173/5174；已有服务不需重复启动。
- 后续建议的检查命令（本轮未执行）：`pnpm run type-check`、`pnpm --filter superform-antdv typecheck`、`pnpm --filter superform-element-plus typecheck`。
- 若后续明确需要单元回归，建议在现有 adapter/elementPlusAdapter 用例中补充 Collapse 延迟内容、默认属性/事件/插槽与统一校验错误，再执行 `pnpm exec vitest run tests/adapter.test.ts tests/elementPlusAdapter.test.ts`；旧用例需要先按当前协议迁移，本轮未调整、未执行。
- Table 编辑状态管理仅在前述定向回归通过并收到用户反馈后继续；不将本轮测试页生成视为该项已完成。

## 2026-09-21 example 按功能整合为开发、演示与人工测试入口

### 需求与影响范围

- 用户明确：以功能演示为主，将原 AntDV 普通示例 #0/#1/#2 的内容纳入新页面；每个 UI 一个入口，旧链接无需保留；页面写明操作和联动关系，右侧可控浮窗观察数据。同时允许整理示例中不合理、不一致之处。
- ~~以测试编号和失败场景作为页面主导航，保留独立 upgrade-dev 入口。~~ 改为七个业务功能页，测试按钮集中在默认折叠的“开发与测试”区，模拟失败、拦截和默认属性冲突均默认关闭。
- 新增 examples/shared 演示壳层、观察窗和功能页面；两套 example 只负责产品初始化、原生语言环境和源码别名。@demo/product 在开发环境指向各自当前源码，构建配置仍走各自产品包。
- 未改变库架构或公开 API，未继续修改 Core/Adapter。已有未提交改动保留；闲置的普通示例、upgrade-dev 源文件和 Element Plus schema.ts 保留现场，当前入口不再导入，扫描与 example 类型检查范围也不再包含它们。移除旧 upgrade-dev/index.html 与 dev:upgrade 启动脚本，不维护第二个浏览器入口。

### 新入口与旧功能覆盖

- AntDV：http://127.0.0.1:5173/#basic；Element Plus：http://127.0.0.1:5174/#basic。顶部可切换 UI，侧边栏切换功能。旧数字 hash 不再有独立含义，未知 hash 回到基础表单。
- 以下是代码中的覆盖映射，并非交互验证通过记录。

| 功能页 | 内容与旧功能对应 |
| --- | --- |
| 基础表单 #basic | 旧 #1 的输入、搜索、数字、选项、单选/多选、开关、评分、标签、日期/时间范围、提交/重置/回填及详情切换；表单整体/局部校验和原生实例入口 |
| 字段联动 #linkage | 旧 #1 的显隐/必填/组级禁用、级联选项、计算值、TreeSelect；补充外部赋值/清空/异步标签、单多选原生与 Schema 事件计数，界面显示联动关系 |
| 容器与列表 #containers | 旧 #1 的 Group/Card/Tabs/Collapse、GroupList 和 InputList；成员 CardList/TabList/CollapseList、弹窗/查看、自定义包装、插槽、增删/激活/锚点；字符串数组、日期对象数组、组级校验与分期累计计算 |
| 表格操作 #tables | 旧 #2 的搜索展开、分类页签、动态条件、分页与选择、增删改/详情、行内/弹窗/整表编辑、动态 editable、异步选项、多选字符串存储、范围/上传/备注、footer、附加列及 AntDV 列宽调整；补充保存失败重试、拦截和外部数据变化按钮 |
| 上传与弹窗 #overlays | 旧 #0 的弹窗表单、弹窗表格、外部批量填充、动态标题和确认/取消；旧 #1 的五种上传模式；提交字段卸载、延迟/失败/重试与调用次数。模拟上传不请求服务器 |
| 详情与扩展 #extensions | 旧 #0/#1 的详情、业务组件、Group 包装、命名/原生插槽、HTML/Hidden、Schema Buttons 与独立 SuperButtons、下拉/确认/折叠/动态禁用、表单内 Descriptions；Tabs/Collapse 默认值覆盖及日期时间同源注册对照 |
| 综合示例 #integrated | 将旧 #1 的表单、内嵌 Table、计算字段、条件字段、日期标签、折叠和上传组织为采购订单，展示客户→商品明细→预览/提交流程及独立提交快照 |

### 数据观察与一致性整理

- ~~观察窗支持开关、收起、拖动、调整尺寸、复位，浮窗层级高于演示弹窗。~~ 用户指出遮挡演示，已改为右侧占位区域，见下节。暂停、清空、复制、当前模型、最近 80 条变化（路径与前后值）、最近 60 条事件/结果保留。
- 切页或重置重建当前示例并清空记录，旧页面异步结果不混入新页面事件；共享对象可重复显示，仅真正循环引用标记截断。文件显示元数据，错误保留 name/message/cause 与统一校验 fields。
- 同一业务 Schema 经两个产品别名运行，差异集中在必要的原生属性（多选、清空、输入类型、事件等）和 UI 初始化；不再复制两套大测试页。
- 修正示例误用：动态禁用使用 Schema disabled，InputSlot 使用 Core 传入绑定，SuperButtons 直接传公开属性；基础表单详情切换保留表单挂载，采购订单 delivery 初值与折叠字段层级一致；开发工具栏样式不覆盖原生表单控件。
- 自动导入只扫描新共享页，动态字段名显式补齐 types；两套 example tsconfig 聚焦实际入口与共享页，避免闲置旧接口阻断后续检查。Element Plus 移除闲置的 registrationProbe 注入。
- Tabs/Collapse 冲突默认值只在 ?defaults=on 启用；Element Plus 同源注册使用 ?registration=auto/original/range。从页面切换会重载，手动来源有可见标记。自动导入去重仍需查看虚拟注册模块，未伪造通过结果。
- Table 本地数组模式与模拟接口模式可切换：本地模式直接暴露 Core 插入/编辑/选择/分页边界；接口模式用于正常演示查询和异步持久化。下一阶段的状态管理问题没有通过示例层掩盖或宣称修复。

### 验证状态与后续操作

- 已执行：仅指定 example 文件的 Prettier 格式化和 git diff --check 静态差异检查；不能替代类型或交互验证。
- 未执行：类型检查、单元测试、浏览器交互验证、install、build。未新增或修改测试文件，未启动新 dev 服务；现有服务若未自动载入配置，需在可见集成终端重启。
- 可用启动命令：在 D:\antdv-superform 执行 pnpm run dev:antdv（5173）或 pnpm run dev:element-plus（5174）；已有服务不要重复启动。
- 建议类型检查（均未执行）：pnpm exec vue-tsc --noEmit -p example-antdv/tsconfig.json；pnpm exec vue-tsc --noEmit -p example-element-plus/tsconfig.json。另按需执行 Core 与两套包已有 type-check/typecheck 命令。
- 用户人工验证顺序：字段 TreeSelect 值/标签与事件、日期范围固定属性/同源注册 → 容器增删刷新/激活/插槽/默认值 → Form 统一校验与上传卸载 → Table 保存失败重试/取消。随后再记录外部数据重排/替换、插入锚点失效、选择与分页同步的实际结果。
- 若之后授权单元回归，建议调整现有 fields、elementPlusAdapter、adapter、ListGroup 用例，覆盖上述实际反馈；可用 pnpm exec vitest run tests/fields.test.ts tests/elementPlusAdapter.test.ts tests/adapter.test.ts tests/ListGroup.test.ts，旧协议用例需先迁移，本轮未调整、未执行。不新增成套测试文件。
- Table 编辑状态管理继续等待定向交互反馈，不将本轮示例整合视为该项完成。

## 2026-09-21 模板解析修复、固定分区与样式隔离

- 用户反馈 DataInspector.vue 的 Vite/Vue 表达式解析错误。根因是模板事件中的连续赋值经无分号格式化后无法被 Vue 当作单一表达式解析；清空记录改用脚本方法，同时修复 Linkage.vue 外部赋值/清空的同类写法。
- ~~品牌位于左侧、页面标题占主内容区域、观察窗悬浮、测试区位于各页内容底部展开。~~ 改为顶部当前页面标题与说明、右上角 SuperForm + UI 名称（点击切换 UI）、中央动态主内容区、右侧独立观察区与底部测试区。
- 右侧收起为窄栏，展开时占用实际布局宽度；移除 fixed、拖动、缩放和高 z-index，不覆盖演示组件。窄屏下按正常文档布局放到下方，仍不悬浮。
- 七页开发/测试内容通过 Teleport 投入统一底部目标，开关始终位于底部，内容向上展开并缩小主内容区。目标先于动态页面挂载；收起仅隐藏测试内容，不销毁其状态。综合示例未提交时显示操作说明，提交后展示快照。
- 移除 :root、通配符、全局 button/input/select/a、整页焦点/禁用样式以及 .sup-title 覆盖。工具按钮、标签和输入框使用明确的 demo-control 类，说明文案用直属子元素选择器，保留原生 UI 组件默认样式。body 仅清除页面外边距，业务自定义包装样式仍仅作用于显式自定义组件。
- 已执行：指定示例文件格式化；用现有 @vue/compiler-sfc 在内存中静态编译 9 个共享 Vue 模板，0 个语法错误；git diff --check 无空白错误。未生成编译产物，不将模板语法检查视为运行时验证。
- 未执行：类型检查、单元测试、浏览器交互验证、install/build；未新增或修改测试文件，未改 Core/Adapter，保留其它未提交改动。
- 建议人工检查：两套入口逐页切换、右侧展开/收起、底部展开/收起、清空记录及 TreeSelect 外部赋值/清空；对比两套 UI 默认按钮/输入/标题/禁用/焦点效果，检查弹窗和窄屏无区域遮挡。上述交互均未执行。
- 建议后续类型命令：pnpm exec vue-tsc --noEmit -p example-antdv/tsconfig.json；pnpm exec vue-tsc --noEmit -p example-element-plus/tsconfig.json，均未执行。本轮不需要新增成套单元测试。

- 品牌位置澄清：~~右上角 SuperForm + UI 名称~~，恢复左上角原有 SF 标识与 SuperForm，第二行由原说明改为 UI 名称；页面标题仍在顶部主内容列。仅调整 DemoApp.vue 与外壳 CSS，本次未运行测试、类型检查或构建；建议人工查看两套入口的品牌两行布局及窄屏换行（未执行），需要类型检查时沿用上方两套 example 的 vue-tsc 命令（未执行）。

- 左栏结构再次澄清：~~品牌仅在横跨全页的顶栏中与菜单对齐~~，改为品牌和菜单共同放入左侧侧栏，贯穿页面高度；右列独立承载顶部标题、主内容、观察区和底部测试区。仅调整外壳结构与响应式 CSS。本次未执行测试、类型检查、浏览器交互或 install/build；建议人工检查左栏整体排列与窄屏布局（未执行），类型检查沿用前述两套 example 的 vue-tsc 命令（未执行）。
