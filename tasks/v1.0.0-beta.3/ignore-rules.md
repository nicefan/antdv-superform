# Form `ignoreRules` 适配

日期：2026-09-24。状态：源码已修改，待人工验证。

必填标记已映射到 AntDV 和 Element Plus 实际支持的表单属性。AntDV 使用 `validateTrigger: false`；普通字段和 InputGroup 的规则在两套 UI 下屏蔽输入时的自动触发，显式 `validate` 和 `submit` 仍校验规则。

本轮未运行测试或构建。
