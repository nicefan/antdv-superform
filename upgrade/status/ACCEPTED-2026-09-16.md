# 2026-09-16 完成与更新归档

状态：已完成。用户明确确认将未提交代码及任务设为完成，并要求回补升级归档、清理活动任务和同步正式文档。此确认取代此前“待人工验收”状态，不代表执行过测试。

## 完成范围

- T001：图标统一使用渲染函数，Core 内置 SVG 与七个默认动作图标；移除图标库直接依赖及多余渲染包装。
- T002：删除按源记录或 rowKey 定位；TableEdit 复用源记录和行模型，移动时更新整棵校验路径；cloneModelsFlat 返回 rootModels。
- T003：useTable 异步包装等待注册，透传多个参数并返回调用结果及异常，公开查询不增加节流。
- T004：SuperDetail 响应整体 schema 替换，统一数据源优先级，无 subItems 时清理旧模型。
- InputGroup：紧凑容器直接接收字段节点，自动宽度参与布局；无字段模板保留规则；通过 exaProvider.validateField 和 Adapter 校验单个分组。
- InputList：保留用户现有普通数组槽位复用与对象行缓存调整；行按钮只携带 parent/index，不携带 propChain。示例以组级 validator 表达“至少填写一项”，并保留子项长度规则。

## 删除的中间方案

- ~~通过 FormItem 实例 onFieldChange 或框架内部 context 触发校验~~：改用现有表单实例的 Adapter 局部校验。
- ~~Collections 按 Buttons 类型排除校验路径~~：从 InputList 行按钮模型源头移除 propChain。
- ~~T001–T004 等待人工确认再同步~~：已收到本次完成及文档同步指令。

## 可观察变化及迁移

图标必须为 () => VNodeChild，组件改为 () => h(Component)，字符串图标改为项目自己的渲染函数；显式 icon: undefined 移除默认图标。第三方 Adapter 语义映射值同样为渲染函数，支持复合字段局部校验时实现 FormAdapter.validateField(instance, path)。

useTable 的 query/reload/goPage 在注册前等待，在注册后返回请求 Promise。SuperDetail 切换 schema 后更新字段，显式 dataSource 优先于 schema.dataSource。编辑表格保持源记录身份；不存在的删除目标不会导致末行误删。

## 文档同步

架构总览、ADR-0007、根与产品 README、AI_GUIDE、迁移说明，以及站点安装、按钮、全局配置、容器、校验、表格、详情和 Adapter 使用说明均已按当前实现同步。站点不增加任务历史流水。

原始任务记录保存在 [completed-tasks](./completed-tasks/)，作为历史需求、实施和当时验证状态的来源；本文件为最终完成状态。

## 建议验证（本次均未执行，未新增或修改测试）

- 图标覆盖/移除与 labelMode；两个 Adapter 的内置动作和业务渲染函数。
- InputGroup 首尾边框、自动宽度、组级与子项规则、报错消退及提交拦截；按钮不占用校验路径。
- 普通数组与对象数组的增删、重排、重复值、嵌套父级移动；行按钮定位和字段写回。
- 表格删除未命中、rowKey 为 0、行编辑模型复用。
- useTable 注册前调用、参数/结果/异常传递及分页语义。
- SuperDetail schema 清空/替换、对象/Ref 数据源优先级、setOption/setData。
- 可按需执行：pnpm test tests/formModel.test.ts tests/formComponents.test.ts tests/adapter.test.ts；pnpm run type-check；pnpm run docs:check-types；pnpm run docs:build。
- 需要界面确认时，可手动运行 pnpm dev:antdv 或 pnpm dev:element-plus；本次没有启动服务。

完成与验证分开记录：用户确认完成，自动化和人工交互回归仍未由助手执行。
