> 最终状态：已完成（2026-09-16 用户明确确认）。正式文档已同步，见 [完成归档](../ACCEPTED-2026-09-16.md)。以下保留当时的实施过程与待办表述作为历史，不再代表活动状态；本次未执行测试或构建。

# T003：useTable调用可靠性

创建日期：2026-09-12
状态：已实现，待人工验收
人工验收：待确认

## 需求与授权

移植 main 的注册等待和异步返回逻辑。
用户已明确授权“好的先拆成三个任务并执行”。来源为 origin/main 的相关修复，目标为当前 next-doc；采用逻辑移植，不整批合并提交。

## 范围与设计影响

asyncCall 支持透传多个参数；query、reload、goPage 通过 asyncCall 等待组件注册并返回 Promise。

不改 useQuery，不将公开方法改为节流，不更改卸载取消等待任务行为，不引入 UI 专属类型。

## 实施进度

- [x] 完成范围内代码调整。

仅调整 useTable.ts 的 asyncCall 参数透传和 query/reload/goPage 调用入口，useQuery 保持原样。

- [ ] 人工确认完成。
- [ ] 人工验收后同步必要正式文档。

## 建议验证（未编写、未执行）

注册前调用可在注册后执行；query/reload/goPage 返回结果及异常可 await；query 回第一页、reload 保留页码、goPage 跳页；asyncCall 多参数顺序正确。

按全局约定，本轮不主动新增或修改单元测试、不运行测试、typecheck、install 或 build。建议后续执行类型检查 `pnpm run type-check`，并按上述场景人工验证；如需单元测试，先由用户授权补充相应场景用例后再运行。

## 待同步文档

useTable 方法返回值与注册前调用说明（人工验收后再同步）。

实施授权不等于任务完成验收；确认前仅维护本任务记录。
