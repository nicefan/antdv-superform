> 最终状态：已完成（2026-09-16 用户明确确认）。正式文档已同步，见 [完成归档](../ACCEPTED-2026-09-16.md)。以下保留当时的实施过程与待办表述作为历史，不再代表活动状态；本次未执行测试或构建。

# T002：表格行定位与编辑模型

创建日期：2026-09-12
状态：已实现，待人工验收
人工验收：待确认

## 需求与授权

移植 main 的安全删除和 TableEdit 稳定模型逻辑，适配当前 UIAdapter。
用户已明确授权“好的先拆成三个任务并执行”。来源为 origin/main 的相关修复，目标为当前 next-doc；采用逻辑移植，不整批合并提交。

## 范围与设计影响

Table 删除在源数组内按引用或现有 rowKey 定位，未找到时跳过；TableEdit 直接使用源记录，保留行模型并在索引或父级路径变化时更新整个模型树；cloneModelsFlat 同时返回层级模型。

不引入 useRowKey 新协议，不修改列表组件、查询节流或 CRUD 接口；保留当前 rowKey 对 0 的支持。

## 实施进度

- [x] 完成范围内代码调整。

涉及 Table.vue、TableEdit.tsx、buildData.ts 和 buildModel.ts；删除仅在源数组查找命中后执行，平铺与层级模型共用同一批字段对象。

- [ ] 人工确认完成。
- [ ] 人工验收后同步必要正式文档。

## 建议验证（未编写、未执行）

删除不在数组内的行不误删末行；同 rowKey 不同引用可正确删除；编辑表格增删、重排及嵌套父级移动后字段写回和校验路径正确；两个 Adapter 下保持源记录引用。

按全局约定，本轮不主动新增或修改单元测试、不运行测试、typecheck、install 或 build。建议后续执行类型检查 `pnpm run type-check`，并按上述场景人工验证；如需单元测试，先由用户授权补充相应场景用例后再运行。

## 待同步文档

表格编辑说明与架构中的模型复用约定（人工验收后再同步）。

实施授权不等于任务完成验收；确认前仅维护本任务记录。
